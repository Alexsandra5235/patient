package com.example.patientaccounting.repository;

import com.example.patientaccounting.models.LogDischarge;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogDischargeRepository extends JpaRepository<LogDischarge, Long> {
}
